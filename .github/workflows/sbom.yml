name: SBOM

on:
  push:
    branches:
      - main

jobs:

  sbom:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v6

      - uses: anchore/sbom-action@v0
        with:
          path: .